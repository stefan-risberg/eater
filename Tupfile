include_rules
CXXFLAGS += -I./inc
CXXFLAGS += -I./lib/cppformat
CXXFLAGS += -I/usr/include/soci

GLOBAL_LIBS += -lsqlite3
LIBS += ./lib/cppformat/libformat.a
#LIBS += ./bin/libformat.a

PROJECT_NAME = eater

: foreach src/*.cpp |> !cc |>
: $(BUILD_DIR)/*.o $(LIBS) |> $(CXX) -o %o %f $(GLOBAL_LIBS) |> $(PROJECT_NAME)
